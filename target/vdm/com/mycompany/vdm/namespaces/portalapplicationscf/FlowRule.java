
/*
 * Generated by OData VDM code generator of SAP Cloud SDK in version 3.69.0
 */

package com.mycompany.vdm.namespaces.portalapplicationscf;

import java.util.Map;
import javax.annotation.Nonnull;
import javax.annotation.Nullable;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.google.common.collect.Maps;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.mycompany.vdm.namespaces.portalapplicationscf.field.FlowRuleField;
import com.mycompany.vdm.namespaces.portalapplicationscf.link.FlowRuleOneToOneLink;
import com.mycompany.vdm.namespaces.portalapplicationscf.selectable.FlowRuleSelectable;
import com.sap.cloud.sdk.cloudplatform.connectivity.HttpClientAccessor;
import com.sap.cloud.sdk.cloudplatform.connectivity.HttpDestinationProperties;
import com.sap.cloud.sdk.datamodel.odata.helper.ODataTypeValueSerializer;
import com.sap.cloud.sdk.datamodel.odata.helper.VdmEntity;
import com.sap.cloud.sdk.odatav2.connectivity.ODataException;
import com.sap.cloud.sdk.odatav2.connectivity.ODataExceptionType;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQuery;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQueryBuilder;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQueryResult;
import com.sap.cloud.sdk.s4hana.datamodel.odata.adapter.ODataField;
import com.sap.cloud.sdk.s4hana.datamodel.odata.annotation.Key;
import com.sap.cloud.sdk.typeconverter.TypeConverter;
import io.vavr.control.Option;
import lombok.AccessLevel;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import lombok.ToString;
import org.apache.http.client.HttpClient;
import org.apache.olingo.odata2.api.edm.EdmSimpleTypeKind;


/**
 * <p>Original entity name from the Odata EDM: <b>FlowRule</b></p>
 * 
 */
@Builder
@Data
@NoArgsConstructor
@AllArgsConstructor
@ToString(doNotUseGetters = true, callSuper = true)
@EqualsAndHashCode(doNotUseGetters = true, callSuper = true)
@JsonAdapter(com.sap.cloud.sdk.s4hana.datamodel.odata.adapter.ODataVdmEntityAdapterFactory.class)
public class FlowRule
    extends VdmEntity<FlowRule>
{

    /**
     * Selector for all available fields of FlowRule.
     * 
     */
    public final static FlowRuleSelectable ALL_FIELDS = () -> "*";
    /**
     * (Key Field) Constraints: Not nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>id</b></p>
     * 
     * @return
     *     The id contained in this entity.
     */
    @Key
    @SerializedName("id")
    @JsonProperty("id")
    @Nullable
    @ODataField(odataName = "id")
    private String id;
    /**
     * Use with available fluent helpers to apply the <b>id</b> field to query operations.
     * 
     */
    public final static FlowRuleField<String> ID = new FlowRuleField<String>("id");
    /**
     * Constraints: Nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>FK_REQUEST_ID</b></p>
     * 
     * @return
     *     The fK_REQUEST_ID contained in this entity.
     */
    @SerializedName("FK_REQUEST_ID")
    @JsonProperty("FK_REQUEST_ID")
    @Nullable
    @ODataField(odataName = "FK_REQUEST_ID")
    private String fK_REQUEST_ID;
    /**
     * Use with available fluent helpers to apply the <b>FK_REQUEST_ID</b> field to query operations.
     * 
     */
    public final static FlowRuleField<String> FKREQUESTID = new FlowRuleField<String>("FK_REQUEST_ID");
    /**
     * Constraints: Nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>FK_RESPONSE_ID</b></p>
     * 
     * @return
     *     The fK_RESPONSE_ID contained in this entity.
     */
    @SerializedName("FK_RESPONSE_ID")
    @JsonProperty("FK_RESPONSE_ID")
    @Nullable
    @ODataField(odataName = "FK_RESPONSE_ID")
    private String fK_RESPONSE_ID;
    /**
     * Use with available fluent helpers to apply the <b>FK_RESPONSE_ID</b> field to query operations.
     * 
     */
    public final static FlowRuleField<String> FKRESPONSEID = new FlowRuleField<String>("FK_RESPONSE_ID");
    /**
     * Navigation property <b>request</b> for <b>FlowRule</b> to single <b>Stream</b>.
     * 
     */
    @SerializedName("request")
    @JsonProperty("request")
    @ODataField(odataName = "request")
    @Nullable
    @Getter(AccessLevel.NONE)
    @Setter(AccessLevel.NONE)
    private Stream toRequest;
    /**
     * Navigation property <b>response</b> for <b>FlowRule</b> to single <b>Stream</b>.
     * 
     */
    @SerializedName("response")
    @JsonProperty("response")
    @ODataField(odataName = "response")
    @Nullable
    @Getter(AccessLevel.NONE)
    @Setter(AccessLevel.NONE)
    private Stream toResponse;
    /**
     * Use with available fluent helpers to apply the <b>request</b> navigation property to query operations.
     * 
     */
    public final static FlowRuleOneToOneLink<Stream> TO_REQUEST = new FlowRuleOneToOneLink<Stream>("request");
    /**
     * Use with available fluent helpers to apply the <b>response</b> navigation property to query operations.
     * 
     */
    public final static FlowRuleOneToOneLink<Stream> TO_RESPONSE = new FlowRuleOneToOneLink<Stream>("response");

    @Nonnull
    @Override
    public Class<FlowRule> getType() {
        return FlowRule.class;
    }

    /**
     * (Key Field) Constraints: Not nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>id</b></p>
     * 
     * @param id
     *     The id to set.
     */
    public void setId(
        @Nullable
        final String id) {
        rememberChangedField("id", this.id);
        this.id = id;
    }

    /**
     * Constraints: Nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>FK_REQUEST_ID</b></p>
     * 
     * @param fK_REQUEST_ID
     *     The fK_REQUEST_ID to set.
     */
    public void setFK_REQUEST_ID(
        @Nullable
        final String fK_REQUEST_ID) {
        rememberChangedField("FK_REQUEST_ID", this.fK_REQUEST_ID);
        this.fK_REQUEST_ID = fK_REQUEST_ID;
    }

    /**
     * Constraints: Nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>FK_RESPONSE_ID</b></p>
     * 
     * @param fK_RESPONSE_ID
     *     The fK_RESPONSE_ID to set.
     */
    public void setFK_RESPONSE_ID(
        @Nullable
        final String fK_RESPONSE_ID) {
        rememberChangedField("FK_RESPONSE_ID", this.fK_RESPONSE_ID);
        this.fK_RESPONSE_ID = fK_RESPONSE_ID;
    }

    @Override
    protected String getEntityCollection() {
        return "FlowRules";
    }

    @Nonnull
    @Override
    protected Map<String, Object> getKey() {
        final Map<String, Object> result = Maps.newHashMap();
        result.put("id", getId());
        return result;
    }

    @Nonnull
    @Override
    protected Map<String, Object> toMapOfFields() {
        final Map<String, Object> values = super.toMapOfFields();
        values.put("id", getId());
        values.put("FK_REQUEST_ID", getFK_REQUEST_ID());
        values.put("FK_RESPONSE_ID", getFK_RESPONSE_ID());
        return values;
    }

    @Override
    protected void fromMap(final Map<String, Object> inputValues) {
        final Map<String, Object> values = Maps.newHashMap(inputValues);
        // simple properties
        {
            if (values.containsKey("id")) {
                final Object value = values.remove("id");
                if ((value == null)||(!value.equals(getId()))) {
                    setId(((String) value));
                }
            }
            if (values.containsKey("FK_REQUEST_ID")) {
                final Object value = values.remove("FK_REQUEST_ID");
                if ((value == null)||(!value.equals(getFK_REQUEST_ID()))) {
                    setFK_REQUEST_ID(((String) value));
                }
            }
            if (values.containsKey("FK_RESPONSE_ID")) {
                final Object value = values.remove("FK_RESPONSE_ID");
                if ((value == null)||(!value.equals(getFK_RESPONSE_ID()))) {
                    setFK_RESPONSE_ID(((String) value));
                }
            }
        }
        // structured properties
        {
        }
        // navigation properties
        {
            if ((values).containsKey("request")) {
                final Object value = (values).remove("request");
                if (value instanceof Map) {
                    if (toRequest == null) {
                        toRequest = new Stream();
                    }
                    @SuppressWarnings("unchecked")
                    final Map<String, Object> inputMap = ((Map<String, Object> ) value);
                    toRequest.fromMap(inputMap);
                }
            }
            if ((values).containsKey("response")) {
                final Object value = (values).remove("response");
                if (value instanceof Map) {
                    if (toResponse == null) {
                        toResponse = new Stream();
                    }
                    @SuppressWarnings("unchecked")
                    final Map<String, Object> inputMap = ((Map<String, Object> ) value);
                    toResponse.fromMap(inputMap);
                }
            }
        }
        super.fromMap(values);
    }

    /**
     * Use with available fluent helpers to apply an extension field to query operations.
     * 
     * @param fieldName
     *     The name of the extension field as returned by the OData service.
     * @param <T>
     *     The type of the extension field when performing value comparisons.
     * @param fieldType
     *     The Java type to use for the extension field when performing value comparisons.
     * @return
     *     A representation of an extension field from this entity.
     */
    @Nonnull
    public static<T >FlowRuleField<T> field(
        @Nonnull
        final String fieldName,
        @Nonnull
        final Class<T> fieldType) {
        return new FlowRuleField<T>(fieldName);
    }

    /**
     * Use with available fluent helpers to apply an extension field to query operations.
     * 
     * @param typeConverter
     *     A TypeConverter<T, DomainT> instance whose first generic type matches the Java type of the field
     * @param fieldName
     *     The name of the extension field as returned by the OData service.
     * @param <T>
     *     The type of the extension field when performing value comparisons.
     * @param <DomainT>
     *     The type of the extension field as returned by the OData service.
     * @return
     *     A representation of an extension field from this entity, holding a reference to the given TypeConverter.
     */
    @Nonnull
    public static<T,DomainT >FlowRuleField<T> field(
        @Nonnull
        final String fieldName,
        @Nonnull
        final TypeConverter<T, DomainT> typeConverter) {
        return new FlowRuleField<T>(fieldName, typeConverter);
    }

    @Override
    @Nullable
    public HttpDestinationProperties getDestinationForFetch() {
        return super.getDestinationForFetch();
    }

    @Override
    protected void setServicePathForFetch(
        @Nullable
        final String servicePathForFetch) {
        super.setServicePathForFetch(servicePathForFetch);
    }

    @Override
    public void attachToService(
        @Nullable
        final String servicePath,
        @Nonnull
        final HttpDestinationProperties destination) {
        super.attachToService(servicePath, destination);
    }

    @Override
    protected String getDefaultServicePath() {
        return (com.mycompany.vdm.services.APIPortalApplicationsCFService.DEFAULT_SERVICE_PATH);
    }

    @Nonnull
    @Override
    protected Map<String, Object> toMapOfNavigationProperties() {
        final Map<String, Object> values = super.toMapOfNavigationProperties();
        if (toRequest!= null) {
            (values).put("request", toRequest);
        }
        if (toResponse!= null) {
            (values).put("response", toResponse);
        }
        return values;
    }

    /**
     * Fetches the <b>Stream</b> entity (one to one) associated with this entity. This corresponds to the OData navigation property <b>request</b>.
     * <p>
     * Please note: This method will not cache or persist the query results.
     * 
     * @return
     *     The single associated <b>Stream</b> entity, or {@code null} if an entity is not associated. 
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nullable
    public Stream fetchRequest()
        throws ODataException
    {
        final HttpDestinationProperties destination = getDestinationForFetch();
        if (destination == null) {
            throw new ODataException(ODataExceptionType.OTHER, "Failed to fetch related objects of type Stream.", new IllegalStateException("Unable to execute OData query. The entity was created locally without an assigned HttpDestination. This method is applicable only on entities which were retrieved or created using the OData VDM."));
        }
        final StringBuilder odataResourceUrl = new StringBuilder(getEntityCollection());
        odataResourceUrl.append("(");
        odataResourceUrl.append("id=");
        odataResourceUrl.append(ODataTypeValueSerializer.of(EdmSimpleTypeKind.String).toUri(id));
        odataResourceUrl.append(")/");
        odataResourceUrl.append("request");
        final ODataQueryBuilder builder = ODataQueryBuilder.withEntity(getServicePathForFetch(), odataResourceUrl.toString()).withoutMetadata();
        final ODataQuery query = builder.build();
        final HttpClient httpClient = HttpClientAccessor.getHttpClient(destination);
        final ODataQueryResult result = query.execute(httpClient);
        final Stream entity = result.as(Stream.class);
        entity.attachToService(getServicePathForFetch(), destination);
        return entity;
    }

    /**
     * Retrieval of associated <b>Stream</b> entity (one to one). This corresponds to the OData navigation property <b>request</b>.
     * <p>
     * If the navigation property <b>request</b> of a queried <b>FlowRule</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @return
     *     List of associated <b>Stream</b> entity.
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nullable
    public Stream getRequestOrFetch()
        throws ODataException
    {
        if (toRequest == null) {
            toRequest = fetchRequest();
        }
        return toRequest;
    }

    /**
     * Retrieval of associated <b>Stream</b> entity (one to one). This corresponds to the OData navigation property <b>request</b>.
     * <p>
     * If the navigation property for an entity <b>FlowRule</b> has not been resolved yet, this method will <b>not query</b> further information. Instead its <code>Option</code> result state will be <code>empty</code>.
     * 
     * @return
     *     If the information for navigation property <b>request</b> is already loaded, the result will contain the <b>Stream</b> entity. If not, an <code>Option</code> with result state <code>empty</code> is returned.
     */
    @Nonnull
    public Option<Stream> getRequestIfPresent() {
        return Option.of(toRequest);
    }

    /**
     * Overwrites the associated <b>Stream</b> entity for the loaded navigation property <b>request</b>.
     * 
     * @param value
     *     New <b>Stream</b> entity.
     */
    public void setRequest(final Stream value) {
        toRequest = value;
    }

    /**
     * Fetches the <b>Stream</b> entity (one to one) associated with this entity. This corresponds to the OData navigation property <b>response</b>.
     * <p>
     * Please note: This method will not cache or persist the query results.
     * 
     * @return
     *     The single associated <b>Stream</b> entity, or {@code null} if an entity is not associated. 
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nullable
    public Stream fetchResponse()
        throws ODataException
    {
        final HttpDestinationProperties destination = getDestinationForFetch();
        if (destination == null) {
            throw new ODataException(ODataExceptionType.OTHER, "Failed to fetch related objects of type Stream.", new IllegalStateException("Unable to execute OData query. The entity was created locally without an assigned HttpDestination. This method is applicable only on entities which were retrieved or created using the OData VDM."));
        }
        final StringBuilder odataResourceUrl = new StringBuilder(getEntityCollection());
        odataResourceUrl.append("(");
        odataResourceUrl.append("id=");
        odataResourceUrl.append(ODataTypeValueSerializer.of(EdmSimpleTypeKind.String).toUri(id));
        odataResourceUrl.append(")/");
        odataResourceUrl.append("response");
        final ODataQueryBuilder builder = ODataQueryBuilder.withEntity(getServicePathForFetch(), odataResourceUrl.toString()).withoutMetadata();
        final ODataQuery query = builder.build();
        final HttpClient httpClient = HttpClientAccessor.getHttpClient(destination);
        final ODataQueryResult result = query.execute(httpClient);
        final Stream entity = result.as(Stream.class);
        entity.attachToService(getServicePathForFetch(), destination);
        return entity;
    }

    /**
     * Retrieval of associated <b>Stream</b> entity (one to one). This corresponds to the OData navigation property <b>response</b>.
     * <p>
     * If the navigation property <b>response</b> of a queried <b>FlowRule</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @return
     *     List of associated <b>Stream</b> entity.
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nullable
    public Stream getResponseOrFetch()
        throws ODataException
    {
        if (toResponse == null) {
            toResponse = fetchResponse();
        }
        return toResponse;
    }

    /**
     * Retrieval of associated <b>Stream</b> entity (one to one). This corresponds to the OData navigation property <b>response</b>.
     * <p>
     * If the navigation property for an entity <b>FlowRule</b> has not been resolved yet, this method will <b>not query</b> further information. Instead its <code>Option</code> result state will be <code>empty</code>.
     * 
     * @return
     *     If the information for navigation property <b>response</b> is already loaded, the result will contain the <b>Stream</b> entity. If not, an <code>Option</code> with result state <code>empty</code> is returned.
     */
    @Nonnull
    public Option<Stream> getResponseIfPresent() {
        return Option.of(toResponse);
    }

    /**
     * Overwrites the associated <b>Stream</b> entity for the loaded navigation property <b>response</b>.
     * 
     * @param value
     *     New <b>Stream</b> entity.
     */
    public void setResponse(final Stream value) {
        toResponse = value;
    }


    /**
     * Helper class to allow for fluent creation of FlowRule instances.
     * 
     */
    public final static class FlowRuleBuilder {

        private Stream toRequest;
        private Stream toResponse;

        private FlowRule.FlowRuleBuilder toRequest(final Stream value) {
            toRequest = value;
            return this;
        }

        /**
         * Navigation property <b>request</b> for <b>FlowRule</b> to single <b>Stream</b>.
         * 
         * @param value
         *     The Stream to build this FlowRule with.
         * @return
         *     This Builder to allow for a fluent interface.
         */
        @Nonnull
        public FlowRule.FlowRuleBuilder request(final Stream value) {
            return toRequest(value);
        }

        private FlowRule.FlowRuleBuilder toResponse(final Stream value) {
            toResponse = value;
            return this;
        }

        /**
         * Navigation property <b>response</b> for <b>FlowRule</b> to single <b>Stream</b>.
         * 
         * @param value
         *     The Stream to build this FlowRule with.
         * @return
         *     This Builder to allow for a fluent interface.
         */
        @Nonnull
        public FlowRule.FlowRuleBuilder response(final Stream value) {
            return toResponse(value);
        }

    }

}
