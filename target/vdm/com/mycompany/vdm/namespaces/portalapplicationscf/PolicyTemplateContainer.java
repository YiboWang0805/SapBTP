
/*
 * Generated by OData VDM code generator of SAP Cloud SDK in version 3.69.0
 */

package com.mycompany.vdm.namespaces.portalapplicationscf;

import java.util.List;
import java.util.Map;
import javax.annotation.Nonnull;
import javax.annotation.Nullable;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.google.common.collect.Lists;
import com.google.common.collect.Maps;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.mycompany.vdm.namespaces.portalapplicationscf.field.PolicyTemplateContainerField;
import com.mycompany.vdm.namespaces.portalapplicationscf.link.PolicyTemplateContainerLink;
import com.mycompany.vdm.namespaces.portalapplicationscf.selectable.PolicyTemplateContainerSelectable;
import com.sap.cloud.sdk.cloudplatform.connectivity.HttpClientAccessor;
import com.sap.cloud.sdk.cloudplatform.connectivity.HttpDestinationProperties;
import com.sap.cloud.sdk.datamodel.odata.helper.ODataTypeValueSerializer;
import com.sap.cloud.sdk.datamodel.odata.helper.VdmEntity;
import com.sap.cloud.sdk.odatav2.connectivity.ODataException;
import com.sap.cloud.sdk.odatav2.connectivity.ODataExceptionType;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQuery;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQueryBuilder;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQueryResult;
import com.sap.cloud.sdk.s4hana.datamodel.odata.adapter.ODataField;
import com.sap.cloud.sdk.s4hana.datamodel.odata.annotation.Key;
import com.sap.cloud.sdk.typeconverter.TypeConverter;
import io.vavr.control.Option;
import lombok.AccessLevel;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import lombok.ToString;
import org.apache.http.client.HttpClient;
import org.apache.olingo.odata2.api.edm.EdmSimpleTypeKind;


/**
 * <p>Original entity name from the Odata EDM: <b>PolicyTemplateContainer</b></p>
 * 
 */
@Builder
@Data
@NoArgsConstructor
@AllArgsConstructor
@ToString(doNotUseGetters = true, callSuper = true)
@EqualsAndHashCode(doNotUseGetters = true, callSuper = true)
@JsonAdapter(com.sap.cloud.sdk.s4hana.datamodel.odata.adapter.ODataVdmEntityAdapterFactory.class)
public class PolicyTemplateContainer
    extends VdmEntity<PolicyTemplateContainer>
{

    /**
     * Selector for all available fields of PolicyTemplateContainer.
     * 
     */
    public final static PolicyTemplateContainerSelectable ALL_FIELDS = () -> "*";
    /**
     * Constraints: Nullable, Maximum length: 4096 <p>Original property name from the Odata EDM: <b>description</b></p>
     * 
     * @return
     *     The description contained in this entity.
     */
    @SerializedName("description")
    @JsonProperty("description")
    @Nullable
    @ODataField(odataName = "description")
    private String description;
    /**
     * Use with available fluent helpers to apply the <b>description</b> field to query operations.
     * 
     */
    public final static PolicyTemplateContainerField<String> DESCRIPTION = new PolicyTemplateContainerField<String>("description");
    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>life_cycle</b></p>
     * 
     * @return
     *     The life_cycle contained in this entity.
     */
    @SerializedName("life_cycle")
    @JsonProperty("life_cycle")
    @Nullable
    @ODataField(odataName = "life_cycle")
    private History life_cycle;
    /**
     * (Key Field) Constraints: Not nullable, Maximum length: 255 <p>Original property name from the Odata EDM: <b>name</b></p>
     * 
     * @return
     *     The name contained in this entity.
     */
    @Key
    @SerializedName("name")
    @JsonProperty("name")
    @Nullable
    @ODataField(odataName = "name")
    private String name;
    /**
     * Use with available fluent helpers to apply the <b>name</b> field to query operations.
     * 
     */
    public final static PolicyTemplateContainerField<String> NAME = new PolicyTemplateContainerField<String>("name");
    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>proxyEndPointxml</b></p>
     * 
     * @return
     *     The proxyEndPointxml contained in this entity.
     */
    @SerializedName("proxyEndPointxml")
    @JsonProperty("proxyEndPointxml")
    @Nullable
    @ODataField(odataName = "proxyEndPointxml")
    private String proxyEndPointxml;
    /**
     * Use with available fluent helpers to apply the <b>proxyEndPointxml</b> field to query operations.
     * 
     */
    public final static PolicyTemplateContainerField<String> PROXY_END_POINTXML = new PolicyTemplateContainerField<String>("proxyEndPointxml");
    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>targetEndPointxml</b></p>
     * 
     * @return
     *     The targetEndPointxml contained in this entity.
     */
    @SerializedName("targetEndPointxml")
    @JsonProperty("targetEndPointxml")
    @Nullable
    @ODataField(odataName = "targetEndPointxml")
    private String targetEndPointxml;
    /**
     * Use with available fluent helpers to apply the <b>targetEndPointxml</b> field to query operations.
     * 
     */
    public final static PolicyTemplateContainerField<String> TARGET_END_POINTXML = new PolicyTemplateContainerField<String>("targetEndPointxml");
    /**
     * Constraints: Nullable, Maximum length: 255 <p>Original property name from the Odata EDM: <b>title</b></p>
     * 
     * @return
     *     The title contained in this entity.
     */
    @SerializedName("title")
    @JsonProperty("title")
    @Nullable
    @ODataField(odataName = "title")
    private String title;
    /**
     * Use with available fluent helpers to apply the <b>title</b> field to query operations.
     * 
     */
    public final static PolicyTemplateContainerField<String> TITLE = new PolicyTemplateContainerField<String>("title");
    /**
     * Constraints: Nullable, Maximum length: 10 <p>Original property name from the Odata EDM: <b>version</b></p>
     * 
     * @return
     *     The version contained in this entity.
     */
    @SerializedName("version")
    @JsonProperty("version")
    @Nullable
    @ODataField(odataName = "version")
    private String version;
    /**
     * Use with available fluent helpers to apply the <b>version</b> field to query operations.
     * 
     */
    public final static PolicyTemplateContainerField<String> VERSION = new PolicyTemplateContainerField<String>("version");
    /**
     * Navigation property <b>fileResources</b> for <b>PolicyTemplateContainer</b> to multiple <b>TemplateFileResource</b>.
     * 
     */
    @SerializedName("fileResources")
    @JsonProperty("fileResources")
    @ODataField(odataName = "fileResources")
    @Getter(AccessLevel.NONE)
    @Setter(AccessLevel.NONE)
    private List<TemplateFileResource> toFileResources;
    /**
     * Navigation property <b>policies</b> for <b>PolicyTemplateContainer</b> to multiple <b>TemplatePolicy</b>.
     * 
     */
    @SerializedName("policies")
    @JsonProperty("policies")
    @ODataField(odataName = "policies")
    @Getter(AccessLevel.NONE)
    @Setter(AccessLevel.NONE)
    private List<TemplatePolicy> toPolicies;
    /**
     * Use with available fluent helpers to apply the <b>fileResources</b> navigation property to query operations.
     * 
     */
    public final static PolicyTemplateContainerLink<TemplateFileResource> TO_FILE_RESOURCES = new PolicyTemplateContainerLink<TemplateFileResource>("fileResources");
    /**
     * Use with available fluent helpers to apply the <b>policies</b> navigation property to query operations.
     * 
     */
    public final static PolicyTemplateContainerLink<TemplatePolicy> TO_POLICIES = new PolicyTemplateContainerLink<TemplatePolicy>("policies");

    @Nonnull
    @Override
    public Class<PolicyTemplateContainer> getType() {
        return PolicyTemplateContainer.class;
    }

    /**
     * Constraints: Nullable, Maximum length: 4096 <p>Original property name from the Odata EDM: <b>description</b></p>
     * 
     * @param description
     *     The description to set.
     */
    public void setDescription(
        @Nullable
        final String description) {
        rememberChangedField("description", this.description);
        this.description = description;
    }

    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>life_cycle</b></p>
     * 
     * @param life_cycle
     *     The life_cycle to set.
     */
    public void setLife_cycle(
        @Nullable
        final History life_cycle) {
        rememberChangedField("life_cycle", this.life_cycle);
        this.life_cycle = life_cycle;
    }

    /**
     * (Key Field) Constraints: Not nullable, Maximum length: 255 <p>Original property name from the Odata EDM: <b>name</b></p>
     * 
     * @param name
     *     The name to set.
     */
    public void setName(
        @Nullable
        final String name) {
        rememberChangedField("name", this.name);
        this.name = name;
    }

    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>proxyEndPointxml</b></p>
     * 
     * @param proxyEndPointxml
     *     The proxyEndPointxml to set.
     */
    public void setProxyEndPointxml(
        @Nullable
        final String proxyEndPointxml) {
        rememberChangedField("proxyEndPointxml", this.proxyEndPointxml);
        this.proxyEndPointxml = proxyEndPointxml;
    }

    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>targetEndPointxml</b></p>
     * 
     * @param targetEndPointxml
     *     The targetEndPointxml to set.
     */
    public void setTargetEndPointxml(
        @Nullable
        final String targetEndPointxml) {
        rememberChangedField("targetEndPointxml", this.targetEndPointxml);
        this.targetEndPointxml = targetEndPointxml;
    }

    /**
     * Constraints: Nullable, Maximum length: 255 <p>Original property name from the Odata EDM: <b>title</b></p>
     * 
     * @param title
     *     The title to set.
     */
    public void setTitle(
        @Nullable
        final String title) {
        rememberChangedField("title", this.title);
        this.title = title;
    }

    /**
     * Constraints: Nullable, Maximum length: 10 <p>Original property name from the Odata EDM: <b>version</b></p>
     * 
     * @param version
     *     The version to set.
     */
    public void setVersion(
        @Nullable
        final String version) {
        rememberChangedField("version", this.version);
        this.version = version;
    }

    @Override
    protected String getEntityCollection() {
        return "PolicyTemplateContainers";
    }

    @Nonnull
    @Override
    protected Map<String, Object> getKey() {
        final Map<String, Object> result = Maps.newHashMap();
        result.put("name", getName());
        return result;
    }

    @Nonnull
    @Override
    protected Map<String, Object> toMapOfFields() {
        final Map<String, Object> values = super.toMapOfFields();
        values.put("description", getDescription());
        values.put("life_cycle", getLife_cycle());
        values.put("name", getName());
        values.put("proxyEndPointxml", getProxyEndPointxml());
        values.put("targetEndPointxml", getTargetEndPointxml());
        values.put("title", getTitle());
        values.put("version", getVersion());
        return values;
    }

    @Override
    protected void fromMap(final Map<String, Object> inputValues) {
        final Map<String, Object> values = Maps.newHashMap(inputValues);
        // simple properties
        {
            if (values.containsKey("description")) {
                final Object value = values.remove("description");
                if ((value == null)||(!value.equals(getDescription()))) {
                    setDescription(((String) value));
                }
            }
            if (values.containsKey("name")) {
                final Object value = values.remove("name");
                if ((value == null)||(!value.equals(getName()))) {
                    setName(((String) value));
                }
            }
            if (values.containsKey("proxyEndPointxml")) {
                final Object value = values.remove("proxyEndPointxml");
                if ((value == null)||(!value.equals(getProxyEndPointxml()))) {
                    setProxyEndPointxml(((String) value));
                }
            }
            if (values.containsKey("targetEndPointxml")) {
                final Object value = values.remove("targetEndPointxml");
                if ((value == null)||(!value.equals(getTargetEndPointxml()))) {
                    setTargetEndPointxml(((String) value));
                }
            }
            if (values.containsKey("title")) {
                final Object value = values.remove("title");
                if ((value == null)||(!value.equals(getTitle()))) {
                    setTitle(((String) value));
                }
            }
            if (values.containsKey("version")) {
                final Object value = values.remove("version");
                if ((value == null)||(!value.equals(getVersion()))) {
                    setVersion(((String) value));
                }
            }
        }
        // structured properties
        {
            if (values.containsKey("life_cycle")) {
                final Object value = values.remove("life_cycle");
                if (value instanceof Map) {
                    if (getLife_cycle() == null) {
                        setLife_cycle(new History());
                    }
                    @SuppressWarnings("unchecked")
                    final Map<String, Object> inputMap = ((Map<String, Object> ) value);
                    getLife_cycle().fromMap(inputMap);
                }
                if ((value == null)&&(getLife_cycle()!= null)) {
                    setLife_cycle(null);
                }
            }
        }
        // navigation properties
        {
            if ((values).containsKey("fileResources")) {
                final Object value = (values).remove("fileResources");
                if (value instanceof Iterable) {
                    if (toFileResources == null) {
                        toFileResources = Lists.newArrayList();
                    } else {
                        toFileResources = Lists.newArrayList(toFileResources);
                    }
                    int i = 0;
                    for (Object item: ((Iterable<?> ) value)) {
                        if (!(item instanceof Map)) {
                            continue;
                        }
                        TemplateFileResource entity;
                        if (toFileResources.size()>i) {
                            entity = toFileResources.get(i);
                        } else {
                            entity = new TemplateFileResource();
                            toFileResources.add(entity);
                        }
                        i = (i + 1);
                        @SuppressWarnings("unchecked")
                        final Map<String, Object> inputMap = ((Map<String, Object> ) item);
                        entity.fromMap(inputMap);
                    }
                }
            }
            if ((values).containsKey("policies")) {
                final Object value = (values).remove("policies");
                if (value instanceof Iterable) {
                    if (toPolicies == null) {
                        toPolicies = Lists.newArrayList();
                    } else {
                        toPolicies = Lists.newArrayList(toPolicies);
                    }
                    int i = 0;
                    for (Object item: ((Iterable<?> ) value)) {
                        if (!(item instanceof Map)) {
                            continue;
                        }
                        TemplatePolicy entity;
                        if (toPolicies.size()>i) {
                            entity = toPolicies.get(i);
                        } else {
                            entity = new TemplatePolicy();
                            toPolicies.add(entity);
                        }
                        i = (i + 1);
                        @SuppressWarnings("unchecked")
                        final Map<String, Object> inputMap = ((Map<String, Object> ) item);
                        entity.fromMap(inputMap);
                    }
                }
            }
        }
        super.fromMap(values);
    }

    /**
     * Use with available fluent helpers to apply an extension field to query operations.
     * 
     * @param fieldName
     *     The name of the extension field as returned by the OData service.
     * @param <T>
     *     The type of the extension field when performing value comparisons.
     * @param fieldType
     *     The Java type to use for the extension field when performing value comparisons.
     * @return
     *     A representation of an extension field from this entity.
     */
    @Nonnull
    public static<T >PolicyTemplateContainerField<T> field(
        @Nonnull
        final String fieldName,
        @Nonnull
        final Class<T> fieldType) {
        return new PolicyTemplateContainerField<T>(fieldName);
    }

    /**
     * Use with available fluent helpers to apply an extension field to query operations.
     * 
     * @param typeConverter
     *     A TypeConverter<T, DomainT> instance whose first generic type matches the Java type of the field
     * @param fieldName
     *     The name of the extension field as returned by the OData service.
     * @param <T>
     *     The type of the extension field when performing value comparisons.
     * @param <DomainT>
     *     The type of the extension field as returned by the OData service.
     * @return
     *     A representation of an extension field from this entity, holding a reference to the given TypeConverter.
     */
    @Nonnull
    public static<T,DomainT >PolicyTemplateContainerField<T> field(
        @Nonnull
        final String fieldName,
        @Nonnull
        final TypeConverter<T, DomainT> typeConverter) {
        return new PolicyTemplateContainerField<T>(fieldName, typeConverter);
    }

    @Override
    @Nullable
    public HttpDestinationProperties getDestinationForFetch() {
        return super.getDestinationForFetch();
    }

    @Override
    protected void setServicePathForFetch(
        @Nullable
        final String servicePathForFetch) {
        super.setServicePathForFetch(servicePathForFetch);
    }

    @Override
    public void attachToService(
        @Nullable
        final String servicePath,
        @Nonnull
        final HttpDestinationProperties destination) {
        super.attachToService(servicePath, destination);
    }

    @Override
    protected String getDefaultServicePath() {
        return (com.mycompany.vdm.services.APIPortalApplicationsCFService.DEFAULT_SERVICE_PATH);
    }

    @Nonnull
    @Override
    protected Map<String, Object> toMapOfNavigationProperties() {
        final Map<String, Object> values = super.toMapOfNavigationProperties();
        if (toFileResources!= null) {
            (values).put("fileResources", toFileResources);
        }
        if (toPolicies!= null) {
            (values).put("policies", toPolicies);
        }
        return values;
    }

    /**
     * Fetches the <b>TemplateFileResource</b> entities (one to many) associated with this entity. This corresponds to the OData navigation property <b>fileResources</b>.
     * <p>
     * Please note: This method will not cache or persist the query results.
     * 
     * @return
     *     List containing one or more associated <b>TemplateFileResource</b> entities. If no entities are associated then an empty list is returned. 
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nonnull
    public List<TemplateFileResource> fetchFileResources()
        throws ODataException
    {
        final HttpDestinationProperties destination = getDestinationForFetch();
        if (destination == null) {
            throw new ODataException(ODataExceptionType.OTHER, "Failed to fetch related objects of type TemplateFileResource.", new IllegalStateException("Unable to execute OData query. The entity was created locally without an assigned HttpDestination. This method is applicable only on entities which were retrieved or created using the OData VDM."));
        }
        final StringBuilder odataResourceUrl = new StringBuilder(getEntityCollection());
        odataResourceUrl.append("(");
        odataResourceUrl.append("name=");
        odataResourceUrl.append(ODataTypeValueSerializer.of(EdmSimpleTypeKind.String).toUri(name));
        odataResourceUrl.append(")/");
        odataResourceUrl.append("fileResources");
        final ODataQueryBuilder builder = ODataQueryBuilder.withEntity(getServicePathForFetch(), odataResourceUrl.toString()).withoutMetadata();
        final ODataQuery query = builder.build();
        final HttpClient httpClient = HttpClientAccessor.getHttpClient(destination);
        final ODataQueryResult result = query.execute(httpClient);
        final List<TemplateFileResource> entityList = result.asList(TemplateFileResource.class);
        for (TemplateFileResource entity: entityList) {
            entity.attachToService(getServicePathForFetch(), destination);
        }
        return entityList;
    }

    /**
     * Retrieval of associated <b>TemplateFileResource</b> entities (one to many). This corresponds to the OData navigation property <b>fileResources</b>.
     * <p>
     * If the navigation property <b>fileResources</b> of a queried <b>PolicyTemplateContainer</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @return
     *     List of associated <b>TemplateFileResource</b> entities.
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nonnull
    public List<TemplateFileResource> getFileResourcesOrFetch()
        throws ODataException
    {
        if (toFileResources == null) {
            toFileResources = fetchFileResources();
        }
        return toFileResources;
    }

    /**
     * Retrieval of associated <b>TemplateFileResource</b> entities (one to many). This corresponds to the OData navigation property <b>fileResources</b>.
     * <p>
     * If the navigation property for an entity <b>PolicyTemplateContainer</b> has not been resolved yet, this method will <b>not query</b> further information. Instead its <code>Option</code> result state will be <code>empty</code>.
     * 
     * @return
     *     If the information for navigation property <b>fileResources</b> is already loaded, the result will contain the <b>TemplateFileResource</b> entities. If not, an <code>Option</code> with result state <code>empty</code> is returned.
     */
    @Nonnull
    public Option<List<TemplateFileResource>> getFileResourcesIfPresent() {
        return Option.of(toFileResources);
    }

    /**
     * Overwrites the list of associated <b>TemplateFileResource</b> entities for the loaded navigation property <b>fileResources</b>.
     * <p>
     * If the navigation property <b>fileResources</b> of a queried <b>PolicyTemplateContainer</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @param value
     *     List of <b>TemplateFileResource</b> entities.
     */
    public void setFileResources(
        @Nonnull
        final List<TemplateFileResource> value) {
        if (toFileResources == null) {
            toFileResources = Lists.newArrayList();
        }
        toFileResources.clear();
        toFileResources.addAll(value);
    }

    /**
     * Adds elements to the list of associated <b>TemplateFileResource</b> entities. This corresponds to the OData navigation property <b>fileResources</b>.
     * <p>
     * If the navigation property <b>fileResources</b> of a queried <b>PolicyTemplateContainer</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @param entity
     *     Array of <b>TemplateFileResource</b> entities.
     */
    public void addFileResources(TemplateFileResource... entity) {
        if (toFileResources == null) {
            toFileResources = Lists.newArrayList();
        }
        toFileResources.addAll(Lists.newArrayList(entity));
    }

    /**
     * Fetches the <b>TemplatePolicy</b> entities (one to many) associated with this entity. This corresponds to the OData navigation property <b>policies</b>.
     * <p>
     * Please note: This method will not cache or persist the query results.
     * 
     * @return
     *     List containing one or more associated <b>TemplatePolicy</b> entities. If no entities are associated then an empty list is returned. 
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nonnull
    public List<TemplatePolicy> fetchPolicies()
        throws ODataException
    {
        final HttpDestinationProperties destination = getDestinationForFetch();
        if (destination == null) {
            throw new ODataException(ODataExceptionType.OTHER, "Failed to fetch related objects of type TemplatePolicy.", new IllegalStateException("Unable to execute OData query. The entity was created locally without an assigned HttpDestination. This method is applicable only on entities which were retrieved or created using the OData VDM."));
        }
        final StringBuilder odataResourceUrl = new StringBuilder(getEntityCollection());
        odataResourceUrl.append("(");
        odataResourceUrl.append("name=");
        odataResourceUrl.append(ODataTypeValueSerializer.of(EdmSimpleTypeKind.String).toUri(name));
        odataResourceUrl.append(")/");
        odataResourceUrl.append("policies");
        final ODataQueryBuilder builder = ODataQueryBuilder.withEntity(getServicePathForFetch(), odataResourceUrl.toString()).withoutMetadata();
        final ODataQuery query = builder.build();
        final HttpClient httpClient = HttpClientAccessor.getHttpClient(destination);
        final ODataQueryResult result = query.execute(httpClient);
        final List<TemplatePolicy> entityList = result.asList(TemplatePolicy.class);
        for (TemplatePolicy entity: entityList) {
            entity.attachToService(getServicePathForFetch(), destination);
        }
        return entityList;
    }

    /**
     * Retrieval of associated <b>TemplatePolicy</b> entities (one to many). This corresponds to the OData navigation property <b>policies</b>.
     * <p>
     * If the navigation property <b>policies</b> of a queried <b>PolicyTemplateContainer</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @return
     *     List of associated <b>TemplatePolicy</b> entities.
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nonnull
    public List<TemplatePolicy> getPoliciesOrFetch()
        throws ODataException
    {
        if (toPolicies == null) {
            toPolicies = fetchPolicies();
        }
        return toPolicies;
    }

    /**
     * Retrieval of associated <b>TemplatePolicy</b> entities (one to many). This corresponds to the OData navigation property <b>policies</b>.
     * <p>
     * If the navigation property for an entity <b>PolicyTemplateContainer</b> has not been resolved yet, this method will <b>not query</b> further information. Instead its <code>Option</code> result state will be <code>empty</code>.
     * 
     * @return
     *     If the information for navigation property <b>policies</b> is already loaded, the result will contain the <b>TemplatePolicy</b> entities. If not, an <code>Option</code> with result state <code>empty</code> is returned.
     */
    @Nonnull
    public Option<List<TemplatePolicy>> getPoliciesIfPresent() {
        return Option.of(toPolicies);
    }

    /**
     * Overwrites the list of associated <b>TemplatePolicy</b> entities for the loaded navigation property <b>policies</b>.
     * <p>
     * If the navigation property <b>policies</b> of a queried <b>PolicyTemplateContainer</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @param value
     *     List of <b>TemplatePolicy</b> entities.
     */
    public void setPolicies(
        @Nonnull
        final List<TemplatePolicy> value) {
        if (toPolicies == null) {
            toPolicies = Lists.newArrayList();
        }
        toPolicies.clear();
        toPolicies.addAll(value);
    }

    /**
     * Adds elements to the list of associated <b>TemplatePolicy</b> entities. This corresponds to the OData navigation property <b>policies</b>.
     * <p>
     * If the navigation property <b>policies</b> of a queried <b>PolicyTemplateContainer</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @param entity
     *     Array of <b>TemplatePolicy</b> entities.
     */
    public void addPolicies(TemplatePolicy... entity) {
        if (toPolicies == null) {
            toPolicies = Lists.newArrayList();
        }
        toPolicies.addAll(Lists.newArrayList(entity));
    }


    /**
     * Helper class to allow for fluent creation of PolicyTemplateContainer instances.
     * 
     */
    public final static class PolicyTemplateContainerBuilder {

        private List<TemplateFileResource> toFileResources = Lists.newArrayList();
        private List<TemplatePolicy> toPolicies = Lists.newArrayList();

        private PolicyTemplateContainer.PolicyTemplateContainerBuilder toFileResources(final List<TemplateFileResource> value) {
            toFileResources.addAll(value);
            return this;
        }

        /**
         * Navigation property <b>fileResources</b> for <b>PolicyTemplateContainer</b> to multiple <b>TemplateFileResource</b>.
         * 
         * @param value
         *     The TemplateFileResources to build this PolicyTemplateContainer with.
         * @return
         *     This Builder to allow for a fluent interface.
         */
        @Nonnull
        public PolicyTemplateContainer.PolicyTemplateContainerBuilder fileResources(TemplateFileResource... value) {
            return toFileResources(Lists.newArrayList(value));
        }

        private PolicyTemplateContainer.PolicyTemplateContainerBuilder toPolicies(final List<TemplatePolicy> value) {
            toPolicies.addAll(value);
            return this;
        }

        /**
         * Navigation property <b>policies</b> for <b>PolicyTemplateContainer</b> to multiple <b>TemplatePolicy</b>.
         * 
         * @param value
         *     The TemplatePolicys to build this PolicyTemplateContainer with.
         * @return
         *     This Builder to allow for a fluent interface.
         */
        @Nonnull
        public PolicyTemplateContainer.PolicyTemplateContainerBuilder policies(TemplatePolicy... value) {
            return toPolicies(Lists.newArrayList(value));
        }

    }

}
