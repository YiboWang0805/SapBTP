
/*
 * Generated by OData VDM code generator of SAP Cloud SDK in version 3.69.0
 */

package com.mycompany.vdm.namespaces.portalapplicationscf;

import java.util.List;
import java.util.Map;
import javax.annotation.Nonnull;
import javax.annotation.Nullable;
import com.fasterxml.jackson.annotation.JsonProperty;
import com.google.common.collect.Lists;
import com.google.common.collect.Maps;
import com.google.gson.annotations.JsonAdapter;
import com.google.gson.annotations.SerializedName;
import com.mycompany.vdm.namespaces.portalapplicationscf.field.StreamField;
import com.mycompany.vdm.namespaces.portalapplicationscf.link.StreamLink;
import com.mycompany.vdm.namespaces.portalapplicationscf.link.StreamOneToOneLink;
import com.mycompany.vdm.namespaces.portalapplicationscf.selectable.StreamSelectable;
import com.sap.cloud.sdk.cloudplatform.connectivity.HttpClientAccessor;
import com.sap.cloud.sdk.cloudplatform.connectivity.HttpDestinationProperties;
import com.sap.cloud.sdk.datamodel.odata.helper.ODataTypeValueSerializer;
import com.sap.cloud.sdk.datamodel.odata.helper.VdmEntity;
import com.sap.cloud.sdk.odatav2.connectivity.ODataException;
import com.sap.cloud.sdk.odatav2.connectivity.ODataExceptionType;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQuery;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQueryBuilder;
import com.sap.cloud.sdk.odatav2.connectivity.ODataQueryResult;
import com.sap.cloud.sdk.s4hana.datamodel.odata.adapter.ODataField;
import com.sap.cloud.sdk.s4hana.datamodel.odata.annotation.Key;
import com.sap.cloud.sdk.typeconverter.TypeConverter;
import io.vavr.control.Option;
import lombok.AccessLevel;
import lombok.AllArgsConstructor;
import lombok.Builder;
import lombok.Data;
import lombok.EqualsAndHashCode;
import lombok.Getter;
import lombok.NoArgsConstructor;
import lombok.Setter;
import lombok.ToString;
import org.apache.http.client.HttpClient;
import org.apache.olingo.odata2.api.edm.EdmSimpleTypeKind;


/**
 * <p>Original entity name from the Odata EDM: <b>Stream</b></p>
 * 
 */
@Builder
@Data
@NoArgsConstructor
@AllArgsConstructor
@ToString(doNotUseGetters = true, callSuper = true)
@EqualsAndHashCode(doNotUseGetters = true, callSuper = true)
@JsonAdapter(com.sap.cloud.sdk.s4hana.datamodel.odata.adapter.ODataVdmEntityAdapterFactory.class)
public class Stream
    extends VdmEntity<Stream>
{

    /**
     * Selector for all available fields of Stream.
     * 
     */
    public final static StreamSelectable ALL_FIELDS = () -> "*";
    /**
     * Constraints: Nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>FK_FLOW_ID</b></p>
     * 
     * @return
     *     The fK_FLOW_ID contained in this entity.
     */
    @SerializedName("FK_FLOW_ID")
    @JsonProperty("FK_FLOW_ID")
    @Nullable
    @ODataField(odataName = "FK_FLOW_ID")
    private String fK_FLOW_ID;
    /**
     * Use with available fluent helpers to apply the <b>FK_FLOW_ID</b> field to query operations.
     * 
     */
    public final static StreamField<String> FKFLOWID = new StreamField<String>("FK_FLOW_ID");
    /**
     * (Key Field) Constraints: Not nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>id</b></p>
     * 
     * @return
     *     The id contained in this entity.
     */
    @Key
    @SerializedName("id")
    @JsonProperty("id")
    @Nullable
    @ODataField(odataName = "id")
    private String id;
    /**
     * Use with available fluent helpers to apply the <b>id</b> field to query operations.
     * 
     */
    public final static StreamField<String> ID = new StreamField<String>("id");
    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>isRequest</b></p>
     * 
     * @return
     *     The isRequest contained in this entity.
     */
    @SerializedName("isRequest")
    @JsonProperty("isRequest")
    @Nullable
    @JsonAdapter(com.sap.cloud.sdk.s4hana.datamodel.odata.adapter.ODataBooleanAdapter.class)
    @ODataField(odataName = "isRequest")
    private Boolean isRequest;
    /**
     * Use with available fluent helpers to apply the <b>isRequest</b> field to query operations.
     * 
     */
    public final static StreamField<Boolean> IS_REQUEST = new StreamField<Boolean>("isRequest");
    /**
     * Navigation property <b>flow</b> for <b>Stream</b> to single <b>FlowRule</b>.
     * 
     */
    @SerializedName("flow")
    @JsonProperty("flow")
    @ODataField(odataName = "flow")
    @Nullable
    @Getter(AccessLevel.NONE)
    @Setter(AccessLevel.NONE)
    private FlowRule toFlow;
    /**
     * Navigation property <b>steps</b> for <b>Stream</b> to multiple <b>Step</b>.
     * 
     */
    @SerializedName("steps")
    @JsonProperty("steps")
    @ODataField(odataName = "steps")
    @Getter(AccessLevel.NONE)
    @Setter(AccessLevel.NONE)
    private List<Step> toSteps;
    /**
     * Use with available fluent helpers to apply the <b>flow</b> navigation property to query operations.
     * 
     */
    public final static StreamOneToOneLink<FlowRule> TO_FLOW = new StreamOneToOneLink<FlowRule>("flow");
    /**
     * Use with available fluent helpers to apply the <b>steps</b> navigation property to query operations.
     * 
     */
    public final static StreamLink<Step> TO_STEPS = new StreamLink<Step>("steps");

    @Nonnull
    @Override
    public Class<Stream> getType() {
        return Stream.class;
    }

    /**
     * Constraints: Nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>FK_FLOW_ID</b></p>
     * 
     * @param fK_FLOW_ID
     *     The fK_FLOW_ID to set.
     */
    public void setFK_FLOW_ID(
        @Nullable
        final String fK_FLOW_ID) {
        rememberChangedField("FK_FLOW_ID", this.fK_FLOW_ID);
        this.fK_FLOW_ID = fK_FLOW_ID;
    }

    /**
     * (Key Field) Constraints: Not nullable, Maximum length: 36 <p>Original property name from the Odata EDM: <b>id</b></p>
     * 
     * @param id
     *     The id to set.
     */
    public void setId(
        @Nullable
        final String id) {
        rememberChangedField("id", this.id);
        this.id = id;
    }

    /**
     * Constraints: none<p>Original property name from the Odata EDM: <b>isRequest</b></p>
     * 
     * @param isRequest
     *     The isRequest to set.
     */
    public void setIsRequest(
        @Nullable
        final Boolean isRequest) {
        rememberChangedField("isRequest", this.isRequest);
        this.isRequest = isRequest;
    }

    @Override
    protected String getEntityCollection() {
        return "Streams";
    }

    @Nonnull
    @Override
    protected Map<String, Object> getKey() {
        final Map<String, Object> result = Maps.newHashMap();
        result.put("id", getId());
        return result;
    }

    @Nonnull
    @Override
    protected Map<String, Object> toMapOfFields() {
        final Map<String, Object> values = super.toMapOfFields();
        values.put("FK_FLOW_ID", getFK_FLOW_ID());
        values.put("id", getId());
        values.put("isRequest", getIsRequest());
        return values;
    }

    @Override
    protected void fromMap(final Map<String, Object> inputValues) {
        final Map<String, Object> values = Maps.newHashMap(inputValues);
        // simple properties
        {
            if (values.containsKey("FK_FLOW_ID")) {
                final Object value = values.remove("FK_FLOW_ID");
                if ((value == null)||(!value.equals(getFK_FLOW_ID()))) {
                    setFK_FLOW_ID(((String) value));
                }
            }
            if (values.containsKey("id")) {
                final Object value = values.remove("id");
                if ((value == null)||(!value.equals(getId()))) {
                    setId(((String) value));
                }
            }
            if (values.containsKey("isRequest")) {
                final Object value = values.remove("isRequest");
                if ((value == null)||(!value.equals(getIsRequest()))) {
                    setIsRequest(((Boolean) value));
                }
            }
        }
        // structured properties
        {
        }
        // navigation properties
        {
            if ((values).containsKey("flow")) {
                final Object value = (values).remove("flow");
                if (value instanceof Map) {
                    if (toFlow == null) {
                        toFlow = new FlowRule();
                    }
                    @SuppressWarnings("unchecked")
                    final Map<String, Object> inputMap = ((Map<String, Object> ) value);
                    toFlow.fromMap(inputMap);
                }
            }
            if ((values).containsKey("steps")) {
                final Object value = (values).remove("steps");
                if (value instanceof Iterable) {
                    if (toSteps == null) {
                        toSteps = Lists.newArrayList();
                    } else {
                        toSteps = Lists.newArrayList(toSteps);
                    }
                    int i = 0;
                    for (Object item: ((Iterable<?> ) value)) {
                        if (!(item instanceof Map)) {
                            continue;
                        }
                        Step entity;
                        if (toSteps.size()>i) {
                            entity = toSteps.get(i);
                        } else {
                            entity = new Step();
                            toSteps.add(entity);
                        }
                        i = (i + 1);
                        @SuppressWarnings("unchecked")
                        final Map<String, Object> inputMap = ((Map<String, Object> ) item);
                        entity.fromMap(inputMap);
                    }
                }
            }
        }
        super.fromMap(values);
    }

    /**
     * Use with available fluent helpers to apply an extension field to query operations.
     * 
     * @param fieldName
     *     The name of the extension field as returned by the OData service.
     * @param <T>
     *     The type of the extension field when performing value comparisons.
     * @param fieldType
     *     The Java type to use for the extension field when performing value comparisons.
     * @return
     *     A representation of an extension field from this entity.
     */
    @Nonnull
    public static<T >StreamField<T> field(
        @Nonnull
        final String fieldName,
        @Nonnull
        final Class<T> fieldType) {
        return new StreamField<T>(fieldName);
    }

    /**
     * Use with available fluent helpers to apply an extension field to query operations.
     * 
     * @param typeConverter
     *     A TypeConverter<T, DomainT> instance whose first generic type matches the Java type of the field
     * @param fieldName
     *     The name of the extension field as returned by the OData service.
     * @param <T>
     *     The type of the extension field when performing value comparisons.
     * @param <DomainT>
     *     The type of the extension field as returned by the OData service.
     * @return
     *     A representation of an extension field from this entity, holding a reference to the given TypeConverter.
     */
    @Nonnull
    public static<T,DomainT >StreamField<T> field(
        @Nonnull
        final String fieldName,
        @Nonnull
        final TypeConverter<T, DomainT> typeConverter) {
        return new StreamField<T>(fieldName, typeConverter);
    }

    @Override
    @Nullable
    public HttpDestinationProperties getDestinationForFetch() {
        return super.getDestinationForFetch();
    }

    @Override
    protected void setServicePathForFetch(
        @Nullable
        final String servicePathForFetch) {
        super.setServicePathForFetch(servicePathForFetch);
    }

    @Override
    public void attachToService(
        @Nullable
        final String servicePath,
        @Nonnull
        final HttpDestinationProperties destination) {
        super.attachToService(servicePath, destination);
    }

    @Override
    protected String getDefaultServicePath() {
        return (com.mycompany.vdm.services.APIPortalApplicationsCFService.DEFAULT_SERVICE_PATH);
    }

    @Nonnull
    @Override
    protected Map<String, Object> toMapOfNavigationProperties() {
        final Map<String, Object> values = super.toMapOfNavigationProperties();
        if (toFlow!= null) {
            (values).put("flow", toFlow);
        }
        if (toSteps!= null) {
            (values).put("steps", toSteps);
        }
        return values;
    }

    /**
     * Fetches the <b>FlowRule</b> entity (one to one) associated with this entity. This corresponds to the OData navigation property <b>flow</b>.
     * <p>
     * Please note: This method will not cache or persist the query results.
     * 
     * @return
     *     The single associated <b>FlowRule</b> entity, or {@code null} if an entity is not associated. 
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nullable
    public FlowRule fetchFlow()
        throws ODataException
    {
        final HttpDestinationProperties destination = getDestinationForFetch();
        if (destination == null) {
            throw new ODataException(ODataExceptionType.OTHER, "Failed to fetch related objects of type FlowRule.", new IllegalStateException("Unable to execute OData query. The entity was created locally without an assigned HttpDestination. This method is applicable only on entities which were retrieved or created using the OData VDM."));
        }
        final StringBuilder odataResourceUrl = new StringBuilder(getEntityCollection());
        odataResourceUrl.append("(");
        odataResourceUrl.append("id=");
        odataResourceUrl.append(ODataTypeValueSerializer.of(EdmSimpleTypeKind.String).toUri(id));
        odataResourceUrl.append(")/");
        odataResourceUrl.append("flow");
        final ODataQueryBuilder builder = ODataQueryBuilder.withEntity(getServicePathForFetch(), odataResourceUrl.toString()).withoutMetadata();
        final ODataQuery query = builder.build();
        final HttpClient httpClient = HttpClientAccessor.getHttpClient(destination);
        final ODataQueryResult result = query.execute(httpClient);
        final FlowRule entity = result.as(FlowRule.class);
        entity.attachToService(getServicePathForFetch(), destination);
        return entity;
    }

    /**
     * Retrieval of associated <b>FlowRule</b> entity (one to one). This corresponds to the OData navigation property <b>flow</b>.
     * <p>
     * If the navigation property <b>flow</b> of a queried <b>Stream</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @return
     *     List of associated <b>FlowRule</b> entity.
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nullable
    public FlowRule getFlowOrFetch()
        throws ODataException
    {
        if (toFlow == null) {
            toFlow = fetchFlow();
        }
        return toFlow;
    }

    /**
     * Retrieval of associated <b>FlowRule</b> entity (one to one). This corresponds to the OData navigation property <b>flow</b>.
     * <p>
     * If the navigation property for an entity <b>Stream</b> has not been resolved yet, this method will <b>not query</b> further information. Instead its <code>Option</code> result state will be <code>empty</code>.
     * 
     * @return
     *     If the information for navigation property <b>flow</b> is already loaded, the result will contain the <b>FlowRule</b> entity. If not, an <code>Option</code> with result state <code>empty</code> is returned.
     */
    @Nonnull
    public Option<FlowRule> getFlowIfPresent() {
        return Option.of(toFlow);
    }

    /**
     * Overwrites the associated <b>FlowRule</b> entity for the loaded navigation property <b>flow</b>.
     * 
     * @param value
     *     New <b>FlowRule</b> entity.
     */
    public void setFlow(final FlowRule value) {
        toFlow = value;
    }

    /**
     * Fetches the <b>Step</b> entities (one to many) associated with this entity. This corresponds to the OData navigation property <b>steps</b>.
     * <p>
     * Please note: This method will not cache or persist the query results.
     * 
     * @return
     *     List containing one or more associated <b>Step</b> entities. If no entities are associated then an empty list is returned. 
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nonnull
    public List<Step> fetchSteps()
        throws ODataException
    {
        final HttpDestinationProperties destination = getDestinationForFetch();
        if (destination == null) {
            throw new ODataException(ODataExceptionType.OTHER, "Failed to fetch related objects of type Step.", new IllegalStateException("Unable to execute OData query. The entity was created locally without an assigned HttpDestination. This method is applicable only on entities which were retrieved or created using the OData VDM."));
        }
        final StringBuilder odataResourceUrl = new StringBuilder(getEntityCollection());
        odataResourceUrl.append("(");
        odataResourceUrl.append("id=");
        odataResourceUrl.append(ODataTypeValueSerializer.of(EdmSimpleTypeKind.String).toUri(id));
        odataResourceUrl.append(")/");
        odataResourceUrl.append("steps");
        final ODataQueryBuilder builder = ODataQueryBuilder.withEntity(getServicePathForFetch(), odataResourceUrl.toString()).withoutMetadata();
        final ODataQuery query = builder.build();
        final HttpClient httpClient = HttpClientAccessor.getHttpClient(destination);
        final ODataQueryResult result = query.execute(httpClient);
        final List<Step> entityList = result.asList(Step.class);
        for (Step entity: entityList) {
            entity.attachToService(getServicePathForFetch(), destination);
        }
        return entityList;
    }

    /**
     * Retrieval of associated <b>Step</b> entities (one to many). This corresponds to the OData navigation property <b>steps</b>.
     * <p>
     * If the navigation property <b>steps</b> of a queried <b>Stream</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @return
     *     List of associated <b>Step</b> entities.
     * @throws ODataException
     *     If the entity is unmanaged, i.e. it has not been retrieved using the OData VDM's services and therefore has no ERP configuration context assigned. An entity is managed if it has been either retrieved using the VDM's services or returned from the VDM's services as the result of a CREATE or UPDATE call. 
     */
    @Nonnull
    public List<Step> getStepsOrFetch()
        throws ODataException
    {
        if (toSteps == null) {
            toSteps = fetchSteps();
        }
        return toSteps;
    }

    /**
     * Retrieval of associated <b>Step</b> entities (one to many). This corresponds to the OData navigation property <b>steps</b>.
     * <p>
     * If the navigation property for an entity <b>Stream</b> has not been resolved yet, this method will <b>not query</b> further information. Instead its <code>Option</code> result state will be <code>empty</code>.
     * 
     * @return
     *     If the information for navigation property <b>steps</b> is already loaded, the result will contain the <b>Step</b> entities. If not, an <code>Option</code> with result state <code>empty</code> is returned.
     */
    @Nonnull
    public Option<List<Step>> getStepsIfPresent() {
        return Option.of(toSteps);
    }

    /**
     * Overwrites the list of associated <b>Step</b> entities for the loaded navigation property <b>steps</b>.
     * <p>
     * If the navigation property <b>steps</b> of a queried <b>Stream</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @param value
     *     List of <b>Step</b> entities.
     */
    public void setSteps(
        @Nonnull
        final List<Step> value) {
        if (toSteps == null) {
            toSteps = Lists.newArrayList();
        }
        toSteps.clear();
        toSteps.addAll(value);
    }

    /**
     * Adds elements to the list of associated <b>Step</b> entities. This corresponds to the OData navigation property <b>steps</b>.
     * <p>
     * If the navigation property <b>steps</b> of a queried <b>Stream</b> is operated lazily, an <b>ODataException</b> can be thrown in case of an OData query error.
     * <p>
     * Please note: <i>Lazy</i> loading of OData entity associations is the process of asynchronous retrieval and persisting of items from a navigation property. If a <i>lazy</i> property is requested by the application for the first time and it has not yet been loaded, an OData query will be run in order to load the missing information and its result will get cached for future invocations.
     * 
     * @param entity
     *     Array of <b>Step</b> entities.
     */
    public void addSteps(Step... entity) {
        if (toSteps == null) {
            toSteps = Lists.newArrayList();
        }
        toSteps.addAll(Lists.newArrayList(entity));
    }


    /**
     * Helper class to allow for fluent creation of Stream instances.
     * 
     */
    public final static class StreamBuilder {

        private FlowRule toFlow;
        private List<Step> toSteps = Lists.newArrayList();

        private Stream.StreamBuilder toFlow(final FlowRule value) {
            toFlow = value;
            return this;
        }

        /**
         * Navigation property <b>flow</b> for <b>Stream</b> to single <b>FlowRule</b>.
         * 
         * @param value
         *     The FlowRule to build this Stream with.
         * @return
         *     This Builder to allow for a fluent interface.
         */
        @Nonnull
        public Stream.StreamBuilder flow(final FlowRule value) {
            return toFlow(value);
        }

        private Stream.StreamBuilder toSteps(final List<Step> value) {
            toSteps.addAll(value);
            return this;
        }

        /**
         * Navigation property <b>steps</b> for <b>Stream</b> to multiple <b>Step</b>.
         * 
         * @param value
         *     The Steps to build this Stream with.
         * @return
         *     This Builder to allow for a fluent interface.
         */
        @Nonnull
        public Stream.StreamBuilder steps(Step... value) {
            return toSteps(Lists.newArrayList(value));
        }

    }

}
